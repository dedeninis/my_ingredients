# 待辦事項

依可執行性排序。完成後把 `[ ]` 改成 `[x]` 即可。

相關文件：[README.md](README.md)｜[產品規格文件](食材管理App_V1.0_產品規格文件.md)

---

## A. 待驗收

- [x] **手機跨裝置同步** —— 2026-08-01 驗收通過
  電腦端上傳、手機端拉取皆確認正常，兩台裝置看到同一份資料。

- [ ] **通知權限實測**
  在實際瀏覽器（非 `file://`）按下「允許通知」，確認設定頁狀態變為「已允許」，且到期食材會跳出系統通知。

---

## B. 建議優先做

- [x] **PWA 化：manifest + service worker** —— 2026-08-01 完成
  補齊 PRD 12 的離線要求。App shell 已快取，斷網時可正常開啟；同步用的 `api.github.com` 不經快取，確保不會讀到過期的雲端資料。
  圖示由 `tools/make-icons.py` 產生，改造型後重跑即可。
  待你實測：手機加到主畫面後，開飛航模式看是否仍能開啟並操作。

- [ ] **條碼掃描**（PRD 7.6）
  四個 Should 項目中唯一不受 AI 供應商決策阻擋的。
  作法：瀏覽器內建 `BarcodeDetector` + Open Food Facts 等免金鑰的開放商品資料庫。
  注意：台灣本地商品覆蓋率可能不佳，查無資料時須依 PRD 7.6 轉為手動輸入。

- [ ] **資料匯出／匯入**（PRD V1.1）
  目前除雲端同步外沒有任何備份手段。設定頁下載 JSON、需要時匯入還原。

---

## C. 待決定

- [ ] **AI 供應商**（PRD 附錄 C 第 3 項）
  阻擋：拍照辨識、語音輸入的自然語言解析，以及料理建議是否升級為 AI 產生。
  純前端沒有後端可保管金鑰，須先決定作法（使用者自帶金鑰／後端代理）。
  ⚠️ 金鑰不得放進 `settings`，該物件會同步到 Gist，而 Secret Gist 並非私密。

- [ ] **條碼商品資料來源的授權與查詢限制**（PRD 附錄 C 第 4 項）

- [ ] **是否支援小數數量**（PRD 附錄 C 第 6 項）
  目前欄位為 `step=0.01`，實際允許小數，但未正式確認過此為預期行為。

- [ ] **即將到期的排序規則**
  目前依 PRD 7.2「相同狀態下最近新增優先」，因此「3 天後到期」可能排在「明天到期」之上。
  程式行為符合規格，若要改成最快到期優先，**須修改的是 PRD 而非程式**。

- [ ] **是否需要真正的私密性**
  Secret Gist 只靠網址難以猜測來保護，取得網址者即可讀取內容。
  若需要真正私密，須改用獨立 private repo + Contents API（多約 15 行，需處理 `sha` 與 base64）。

---

## D. 已知限制

以下為目前架構的必然結果，非缺陷。接受即可，若要解除需要架構調整。

- **提醒只在開啟頁面時送出** —— 網頁無可靠的本機排程通知 API。要真正的背景通知需改用 Capacitor 封裝成原生 App，現有 HTML／JS 幾乎可原封不動沿用。
- **同步為後寫覆蓋** —— 不做自動合併，衝突時由使用者選擇保留哪一份。
- **`file://` 與線上版資料不共通** —— 瀏覽器視為兩個不同來源。
- **無備份** —— 清除瀏覽器資料即遺失，除非已設定雲端同步。

---

## E. 之後再說

- [ ] **語音輸入的規則式解析**
  數量、單位、存放位置、日期都有明確規律，可先做規則式解析，不一定要等 AI。語音轉文字本身可用瀏覽器內建的 Web Speech API。

- [ ] **擴充料理規則表**
  目前 42 條（32 條關鍵字 + 10 條類型保底）。實際使用後覺得建議太少或不合口味時再補。

- [ ] **模組化拆分**
  `index.html` 已 972 行。目前無相依、無建置流程是刻意選擇；等要接入條碼或 AI 等外部相依時再評估。

- [ ] **PRD V2.0**
  帳號與多裝置、家庭共享與成員權限、消耗速度推估、料理後扣庫存、使用與浪費統計。

---

## 建議路徑

先完成 A 的手機驗收 → B 的 PWA（補齊離線要求）→ B 的條碼掃描（唯一不卡決策的功能）。

這三項完成後，V1.0 在技術上就沒有缺口，其餘全部取決於 C 的 AI 供應商決策。

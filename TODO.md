# 待辦事項

依可執行性排序。完成後把 `[ ]` 改成 `[x]` 即可。

相關文件：[README.md](README.md)｜[產品規格文件](食材管理App_V1.0_產品規格文件.md)

---

## A. 待實測（只有你做得到）

程式都已完成，需要真實裝置或真實資料才能驗收。

- [x] **手機跨裝置同步** —— 2026-08-01 通過
  電腦端上傳、手機端拉取皆正常，兩台裝置看到同一份資料。

- [ ] **通知權限**
  在實際瀏覽器按下「允許通知」，確認設定頁狀態變成「已允許」，且到期食材會跳出系統通知。

- [ ] **離線可用**
  手機加到主畫面後開飛航模式，確認 App 仍能開啟並正常新增、瀏覽、搜尋。

- [ ] **條碼掃描**
  Android 手機實際掃一次全聯或家樂福的包裝商品（鮮奶、豆腐、優格這類），確認相機啟動與辨識正常。同一商品掃第二次應自動帶出。

- [ ] **拍照辨識（含發票）**
  設定 Gemini 金鑰後，實際拍一張真發票與一張單一食材／包裝照片，確認品名還原、非食品過濾、保存期限讀取是否堪用。

- [ ] **語音新增** —— 2026-08-03 完成開發，待實機測試
  手機瀏覽器（尤其 Android Chrome 與 iOS Safari）實際說一句話，確認麥克風權限提示、逐字稿即時顯示、Gemini 解析結果是否堪用，以及沒設定金鑰時的錯誤提示是否清楚。

- [ ] **美食日誌**
  實際拍照寫一篇日誌，確認照片壓縮後畫質可接受、清單縮圖載入順暢；手機與電腦各自開一次，確認文字紀錄同步、照片各自只留在拍攝的那台裝置。

- [ ] **依食譜列出還缺什麼** —— 2026-08-03 完成開發，待實機測試
  設定 Gemini 金鑰後，在採買清單頁實際查幾道常見菜（中式、西式都試試），確認食材清單合理、已有／還缺的比對正確，勾選加入採買清單後數量與單位堪用。

---

## B. 尚未實作的功能

- [ ] **資料匯出／匯入**（PRD V1.1）
  目前除雲端同步外沒有任何備份手段。設定頁下載 JSON、需要時匯入還原。照片存在 IndexedDB，若要一併匯出需另外處理。

---

## C. 待你決定

- [ ] **要不要補接 Open Food Facts**（PRD 附錄 C 第 4 項）
  目前只用本機共用條碼記錄。若補接，加在「查記錄」與「轉手動」之間即可，首次掃到進口商品時可省一次打字。免金鑰、CORS 開放，但台灣商品命中率低。

- [x] **是否支援小數數量**（PRD 附錄 C 第 6 項）—— 2026-08-02 決定：支援
  秤重肉品（0.48 公斤）與用掉一半（0.5 顆高麗菜、0.5 盒豆腐）兩種情境都需要。
  輸入框級距依單位而定：公斤 0.1、公克與毫升 1、其餘計數單位 any（上下鍵跳 1，仍可手動輸入小數）。

- [ ] **即將到期的排序規則**
  目前依 PRD 7.2「相同狀態下最近新增優先」，因此「3 天後到期」可能排在「明天到期」之上。
  程式行為符合規格，若要改成最快到期優先，**須修改的是 PRD 而非程式**。

- [ ] **是否需要真正的私密性**
  Secret Gist 只靠網址難以猜測來保護，取得網址者即可讀取內容。
  若需要真正私密，須改用獨立 private repo + Contents API（多約 15 行，需處理 `sha` 與 base64）。

- [ ] **美食日誌的照片要不要跟著同步**
  目前刻意只留在拍攝的裝置（見 README）。若之後想要照片也跨裝置可見，需要另外設計圖片同步機制（例如另開一個用途的 Gist 或物件儲存），並把免費方案的檔案大小限制納入考慮。

---

## D. 已知限制

以下為目前架構的必然結果，非缺陷。接受即可，若要解除需要架構調整。

- **提醒只在開啟頁面時送出** —— 網頁無可靠的本機排程通知 API。要真正的背景通知需改用 Capacitor 封裝成原生 App，現有 HTML／JS 幾乎可原封不動沿用。
- **相機掃描限 Android** —— iOS Safari 沒有 `BarcodeDetector`，會退回手動輸入條碼號碼。要支援 iOS 需引入約 200KB 的解碼函式庫。
- **同步為後寫覆蓋** —— 庫存與採買不做自動合併，衝突時由使用者選擇保留哪一份。（條碼記錄、食材記憶、美食日誌例外，一律合併不覆蓋。）
- **單筆刪除與同步的先後順序** —— 條碼記錄、食材記憶、美食日誌是合併字典，若剛刪除單一筆就手動同步，雲端舊資料可能被拉回來。背景會在幾秒內自動推送刪除結果，實務影響很小。
- **美食日誌照片不同步** —— 只留在拍攝的裝置，其他裝置看得到文字紀錄但看不到照片。
- **`file://` 與線上版資料不共通** —— 瀏覽器視為兩個不同來源。
- **無備份** —— 清除瀏覽器資料即遺失，除非已設定雲端同步；照片存在 IndexedDB，清除瀏覽器資料同樣會遺失。
- **Secret Gist 非私密** —— 取得網址者即可讀取庫存內容。
- **分享到 LINE 不會帶出料理名稱** —— 2026-08-03 實機測試確認。已嘗試已知的 `title` 留空解法（commit `bd9a940`），實測仍無效，之後不再嘗試修。帶圖片時 LINE 似乎完全不讀 `text`／`title`，這超出網頁能控制的範圍。若要解，唯一可靠做法是把名稱直接疊進照片畫面裡（改變分享出去的圖片內容），或另外提供「複製名稱」的剪貼簿備援，兩者都還沒做。

---

## E. 之後再說

- [ ] **首頁「本週動態」小提示**
  2026-08-03 UX 討論時提出的第四個選項，這輪只做了 A（首頁日誌預覽）+ B（從快速操作移出）+ C（分享按鈕改小圖示），這項當時判斷是錦上添花、不急，先擱著。做法是首頁加一行「本週記錄 N 篇日誌」之類的文字，強化「這是持續累積的東西」的感覺。

- [ ] **擴充料理規則表**
  目前 44 條（34 條關鍵字 + 12 條類型保底，含單一肉類／海鮮保底）。本機規則沒有結果時已會自動嘗試 Gemini 動態建議（需設定金鑰），持續擴充規則表的急迫性降低。

- [ ] **模組化拆分**
  `index.html` 已 2,000+ 行。無相依、無建置流程是刻意選擇，但檔案持續變大，之後要再加功能時可評估拆成模組。

- [ ] **PRD V2.0**
  帳號與多裝置、家庭共享與成員權限、消耗速度推估、料理後扣庫存、使用與浪費統計。

---

## PRD V1.0 完成度

| 優先級 | 狀態 |
|---|---|
| **Must** | ✅ 全部完成 |
| **Should** | ✅ 全部完成：條碼掃描、料理建議、拍照辨識（含發票）、語音新增 |
| **Could** | ✅ 全部完成 |

**超出 PRD 範圍另外做的**：雲端同步、共用條碼記錄與食材記憶（PRD 4.2 原列為不包含）、拍照辨識與 AI 動態料理建議、PWA 離線、**美食日誌**（消耗紀錄，含照片）、**依食譜列出還缺什麼**（採買清單頁，AI 查食材清單跟庫存比對，只顯示已有／還缺，不輸出完整食譜）。

## 建議路徑

A 的七項實測 → B 的匯出匯入。
